version: '3.8'

services:
  stt-service:
    build: .
    container_name: stt-service
    ports:
      - "8000:8000"
    command: uvicorn api:app --host 0.0.0.0 --port 8000
    volumes:
      - .:/app
